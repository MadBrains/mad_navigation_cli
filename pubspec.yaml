name: mad_navigation_cli
description: >
  Command-line utility for Mad Navigation.
  Provides tools for generating navigation code, creating routes,
  and extending your project with custom scripts.
version: 1.0.0

environment:
  sdk: ">=3.8.0 <4.0.0"

dependencies:
  analyzer: ">=8.0.0 <9.0.0"
  mad_scripts_base: ^1.0.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6

executables:
  mad_navigation_cli: